package ${package}.${sonModule}.dao;
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import ${package}.${sonModule}.model.${className};


/**
 * ${comments}
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Mapper extends BaseMapper<${className}> {
	
}
